자바스크립트의 프로토타입을 통한 참조는 마치 인터넷구조(다선형구조)를 보는것과 같이 복잡하다.
이 프로토타입을 통해서 원형이 되는 객체와 그 아래의 자식객체들은 유기적으로 그 원형과 체이닝이 되어있는건데 참 복잡하구만...
그렇게되면 객체들은 예를들어 Person()이라는 함수를 이용하여 객체를 생성하면
o1, o2, o3 ... 
생성된 각각의 인스턴스들은 독립되어있는건가?
Person()함수와 Person()함수의 원형이 되는 객체 즉, 프로토타입(원형) 객체는 다른건가?


 